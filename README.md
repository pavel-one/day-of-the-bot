# 🎯 Telegram Bot "Пидор дня"

Telegram бот для выбора случайного участника группы как "пидор дня" с ведением статистики.

## � Миграция на Telebot v3

✅ **Проект был успешно мигрирован с `go-telegram-bot-api` на `gopkg.in/telebot.v3`**

### Основные изменения:

- **Библиотека:** `github.com/go-telegram-bot-api/telegram-bot-api/v5` → `gopkg.in/telebot.v3`
- **Архитектура:** Использование middleware и контекстных обработчиков
- **API:** Современный и удобный интерфейс telebot

## �🚀 Возможности

- ✅ Работает только в группах  
- # � Telegram Bot "Пидор дня"

Telegram бот для выбора случайного участника группы как "пидор дня" с ведением статистики.

## 🚀 Возможности

- ✅ Работает только в группах  
- �🎲 Случайный выбор участника как "пидор дня"
- 📊 Ведение статистики по всем участникам
- 🗄️ Хранение данных в SQLite с использованием Squirrel query builder
- 🔒 Один выбор в день для каждой группы
- 🎨 Система шаблонизации сообщений с fasttemplate
- 🏗️ Чистая архитектура с разделением на слои
- 🛡️ Умная обработка ошибок Telegram API
- 📊 Ведение статистики по всем участникам
- 🗄️ Хранение данных в SQLite с использованием Squirrel query builder
- 🔒 Один выбор в день для каждой группы
- 🎨 Система шаблонизации сообщений с fasttemplate
- 🏗️ Чистая архитектура с разделением на слои
- 🛡️ Умная обработка ошибок Telegram API

## 📋 Команды

- `/pidor` - Выбрать пидора дня
- `/pidorstats` - Показать статистику всех участников  
- `/pidorinfo` - Информация о сегодняшнем пидоре дня
- `/help` - Показать справку

## 🏗️ Архитектура

Проект построен на принципах чистой архитектуры:

```
├── main.go                     # Точка входа: инициализация и запуск
├── internal/
│   ├── bot/                    # Основная структура бота и методы запуска
│   ├── handlers/               # Обработчики сообщений и команд
│   ├── config/                 # Конфигурация через переменные окружения
│   ├── domain/                 # Доменные модели (User, PersonOfTheDay)
│   ├── repository/             # Слой доступа к данным (SQLite + Squirrel)
│   └── templates/              # Система шаблонизации сообщений
├── cmd/
│   └── example/                # Примеры использования шаблонов
├── .vscode/
│   └── launch.json            # Конфигурация отладки VS Code с .env
├── docker-compose.yml         # Docker конфигурация
└── .env                       # Переменные окружения
```

### Технологии

- **Go 1.25.3** - основной язык
- **gopkg.in/telebot.v3** - библиотека для работы с Telegram Bot API
- **SQLite** + **go-sqlite3** - база данных
- **Squirrel** - типобезопасный SQL query builder
- **fasttemplate** - высокопроизводительный шаблонизатор

### Обработка ошибок

Бот включает умную обработку специфичных ошибок Telegram:

- `TOPIC_CLOSED` - автоматическая отправка в основной чат если топик недоступен
- `chat not found` - чат не найден
- `bot was blocked by the user` - бот заблокирован пользователем
- `group chat was upgraded to a supergroup` - группа обновлена до супергруппы
- `bot was kicked from the group chat` - бот исключен из группы
- `message is too long` - сообщение слишком длинное

### Система шаблонизации

Все пользовательские сообщения выводятся через систему шаблонов для улучшения поддерживаемости:

```go
service, _ := templates.NewMessageService()
message := service.PersonSelected(user)
```

Пример использования:
```bash
go run cmd/example/main.go
```

## 🛠️ Установка и запуск

### Предварительные требования

- Go 1.21 или выше
- SQLite3
- Telegram бот токен (получить у [@BotFather](https://t.me/botfather))

### Быстрый старт

1. Клонируйте репозиторий:
```bash
git clone https://github.com/pavel-one/day-of-the-bot.git
cd day-of-the-bot
```

2. Скопируйте пример конфигурации:
```bash
cp .env.development.example .env.development
```

3. Отредактируйте `.env.development` и укажите ваш BOT_TOKEN

4. Запустите бота:
```bash
make run
```

### Сборка и установка

```bash
# Установить зависимости
make deps

# Собрать бинарный файл
make build

# Запустить тесты
make test
```

## 🐳 Запуск с Docker

### Docker Compose (рекомендуется)

1. Скопируйте файл конфигурации:
```bash
cp .env.docker .env
```

2. Отредактируйте `.env` и укажите ваш токен бота

3. Запустите с Docker Compose:
```bash
docker-compose up -d
```

4. Просмотр логов:
```bash
docker-compose logs -f
```

### Обычный Docker

1. Соберите образ:
```bash
docker build -t day-of-the-bot .
```

2. Запустите контейнер:
```bash
docker run -d \
  --name day-of-the-bot \
  -e BOT_TOKEN=your_bot_token_here \
  -v bot_data:/app/data \
  day-of-the-bot
```

## 🔧 Конфигурация

Бот настраивается через переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | **обязательно** |
| `DB_PATH` | Путь к файлу SQLite | `bot.db` |
| `DEBUG` | Режим отладки | `false` |

### Файлы конфигурации

- `.env.development.example` - пример для разработки
- `.env.docker` - пример для Docker
- `.env.development` - ваша локальная конфигурация (создайте из примера)

## 📝 Структура проекта

```
.
├── cmd/
│   └── example/              # Примеры использования шаблонов
├── internal/                 # Внутренняя логика (не экспортируется)
│   ├── bot/                 # Основная структура бота и методы запуска
│   ├── config/              # Конфигурация через переменные окружения
│   ├── domain/              # Доменные модели (User, PersonOfTheDay)
│   ├── handlers/            # Обработчики сообщений и команд
│   ├── repository/          # Слой доступа к данным (SQLite + Squirrel)
│   └── templates/           # Система шаблонизации сообщений
├── .github/
│   └── copilot-instructions.md  # Инструкции для AI ассистентов
├── .vscode/                 # Конфигурация VS Code
│   ├── launch.json         # Конфигурация отладки
│   └── settings.json       # Настройки проекта
├── main.go                  # Точка входа: инициализация и запуск
├── main_test.go            # Тесты
├── go.mod                  # Go модуль
├── go.sum                  # Контрольные суммы зависимостей
├── .env.development.example # Пример конфигурации для разработки
├── .env.docker             # Пример конфигурации для Docker
├── .gitignore              # Игнорируемые Git файлы
├── Dockerfile              # Docker образ
├── docker-compose.yml      # Docker Compose конфигурация
├── Makefile               # Команды для сборки и разработки
└── README.md              # Документация
```

## 🤝 Использование

1. Добавьте бота в вашу Telegram группу
2. Дайте боту права администратора (для чтения списка участников)
3. Используйте команду `/pidor` для выбора человека дня
4. Просматривайте статистику с помощью `/pidorstats`

## 📈 Функции

- **Защита от дублирования**: Один пидор дня за сутки на группу
- **Автоматическое добавление пользователей**: Бот запоминает всех участников группы
- **Статистика**: Подсчет количества раз, когда каждый участник был выбран
- **Безопасность**: Работа только в группах, проверка прав доступа

## 🧑‍💻 Разработка

### Команды Makefile

```bash
make help      # Показать все доступные команды
make deps      # Установить зависимости
make build     # Собрать бота
make run       # Запустить бота
make test      # Запустить тесты
make clean     # Удалить сгенерированные файлы
```

### VS Code

Проект настроен для разработки в VS Code:

- **Отладка**: Настроенные конфигурации в `.vscode/launch.json`
- **Настройки**: Оптимизированные настройки Go в `.vscode/settings.json`
- **Переменные окружения**: 
  - Скопируйте `.env.development.example` в `.env.development` и настройте BOT_TOKEN
  - Используйте конфигурацию отладки для автоматической загрузки переменных

### Тестирование шаблонов

Для тестирования вывода шаблонов без запуска полного бота:

```bash
go run cmd/example/main.go
```

### Архитектура

Проект использует чистую архитектуру:

- **Domain**: Модели предметной области (`internal/domain/`)
- **Repository**: Слой доступа к данным с интерфейсами (`internal/repository/`)
- **Handlers**: Обработчики команд и сообщений (`internal/handlers/`)
- **Templates**: Система шаблонизации (`internal/templates/`)
- **Config**: Конфигурация приложения (`internal/config/`)

## 🗄️ База данных

Бот автоматически создаст SQLite базу данных с таблицами:

- `users` - информация об участниках групп
- `person_of_the_day` - история выборов "человека дня"

## 📄 Лицензия

MIT License

## 🐛 Сообщить об ошибке

Если вы нашли ошибку, создайте [issue](https://github.com/pavel-one/day-of-the-bot/issues) в репозитории.

## 📞 Поддержка

- GitHub: [@pavel-one](https://github.com/pavel-one)